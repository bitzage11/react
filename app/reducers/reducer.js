import { List, Map } from 'immutable';

export default function reducer(state = {}, action) {
    switch (action.type) {
        case 'TEMP':
            return state;
        default:
            return state;

    }
}
